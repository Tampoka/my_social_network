import React from 'react';

export type ProfileStatusPropTypes = {};

const ProfileStatus = (props: ProfileStatusPropTypes) => {
    return (
        <div>
            My status message
        </div>
    );
};


export default ProfileStatus;